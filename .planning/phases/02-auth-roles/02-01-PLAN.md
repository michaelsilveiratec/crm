# Plan: 02-01-AUTH-ROLES

**Phase:** 2 (Authentication & Roles)
**Goal:** Criar tela de login, sistema de JWT e controle de acesso por cargo (Pastor/Obreiro).
**Status:** Ready to execute

## Implementation Tasks

<tasks>

<task id="02-01-01" type="execute" autonomous="true">
<title>Instalar Dependências de Auth</title>
<action>
Executar:
- `go get github.com/golang-jwt/jwt/v5`
- `go get golang.org/x/crypto/bcrypt`
</action>
<read_first>
- .planning/phases/02-auth-roles/02-RESEARCH.md
</read_first>
<acceptance_criteria>
- go.mod contém as bibliotecas jwt e bcrypt.
</acceptance_criteria>
</task>

<task id="02-01-02" type="execute" autonomous="true">
<title>Migrar Tabela de Usuários</title>
<action>
Atualizar `internal/db/db.go` para criar a tabela `users`.
Adicionar um usuário "pastor" padrão para testes (senha: admin123).
</action>
<read_first>
- internal/db/db.go
</read_first>
<acceptance_criteria>
- Tabela `users` existe no `crm.db`.
- Usuário inicial "pastor" existe no banco.
</acceptance_criteria>
</task>

<task id="02-01-03" type="execute" autonomous="true">
<title>Implementar Lógica de JWT e Login (Backend)</title>
<action>
Criar `internal/auth/auth.go`:
- Funções para gerar e validar tokens JWT.
- Middleware `AuthRequired` que lê o cookie HttpOnly.
- Middleware `RoleRequired(role)` para autorização.
Criar rotas em `internal/api/auth_handlers.go`:
- POST `/api/login`
- POST `/api/logout`
- GET `/api/me` (retorna usuário logado)
</action>
<read_first>
- .planning/phases/02-auth-roles/02-RESEARCH.md
</read_first>
<acceptance_criteria>
- Login gera cookie HttpOnly.
- `/api/me` retorna erro 401 sem token e 200 com token válido.
</acceptance_criteria>
</task>

<task id="02-01-04" type="execute" autonomous="true">
<title>Criar Tela de Login (Frontend)</title>
<action>
No Preact (`ui/src`):
- Criar componente `Login.tsx`.
- Implementar estado de autenticação (simples, via Context ou State).
- Integrar com o backend para realizar o login.
</action>
<read_first>
- ui/src/app.tsx
</read_first>
<acceptance_criteria>
- Tela de login aparece quando o usuário não está autenticado.
- Login redireciona para a tela principal.
</acceptance_criteria>
</task>

</tasks>

## Verification Criteria
1. Tentar acessar `/api/me` sem logar (deve dar 401).
2. Fazer login com `pastor/admin123`.
3. Verificar se o cookie `jwt` está presente no navegador.
4. Tentar acessar `/api/me` após login (deve dar 200).
