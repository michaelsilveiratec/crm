# Plan: 03-01-MEMBER-INTAKE

**Phase:** 3 (Member Intake)
**Goal:** Criar o formulário de cadastro de visitantes e membros e a API correspondente.
**Status:** Ready to execute

## Implementation Tasks

<tasks>

<task id="03-01-01" type="execute" autonomous="true">
<title>Atualizar Schema do Banco (Tabela members)</title>
<action>
Atualizar `internal/db/db.go`:
- Adicionar CREATE TABLE `members` no `InitDB`.
- Adicionar struct `Member`.
- Implementar função `CreateMember(member *Member) error`.
</action>
<read_first>
- internal/db/db.go
</read_first>
<acceptance_criteria>
- Tabela `members` criada no SQLite.
- Função `CreateMember` inserindo dados corretamente.
</acceptance_criteria>
</task>

<task id="03-01-02" type="execute" autonomous="true">
<title>Implementar API de Cadastro (Backend)</title>
<action>
Criar `internal/api/member_handlers.go`:
- Implementar `CreateMemberHandler`.
- Validar campos obrigatórios (Nome, WhatsApp).
- Vincular o cadastro ao `UserID` de quem está logado.
Atualizar `cmd/server/main.go` para registrar a rota `POST /api/members`.
</action>
<read_first>
- cmd/server/main.go
- internal/api/auth_handlers.go
</read_first>
<acceptance_criteria>
- Rota `/api/members` responde a requisições POST.
- Retorna 201 Created em caso de sucesso.
</acceptance_criteria>
</task>

<task id="03-01-03" type="execute" autonomous="true">
<title>Desenvolver Componente de Formulário (Frontend)</title>
<action>
Criar `ui/src/components/MemberForm.tsx`:
- Formulário com todos os campos solicitados (Nome, Endereço, etc.).
- Uso de Pico.css para estilização básica.
- Feedback visual de carregamento e sucesso.
</action>
<read_first>
- ui/src/App.tsx
</read_first>
<acceptance_criteria>
- Formulário renderiza corretamente.
- Botão "Cadastrar" envia os dados para a API.
- Exibe mensagem de "Cadastro realizado com sucesso!".
</acceptance_criteria>
</task>

<task id="03-01-04" type="execute" autonomous="true">
<title>Integrar Formulário na App</title>
<action>
Atualizar `ui/src/App.tsx`:
- Exibir `MemberForm` após o login.
- Garantir que apenas usuários autenticados vejam o formulário.
</action>
<read_first>
- ui/src/App.tsx
</read_first>
<acceptance_criteria>
- Após login, o formulário de cadastro é exibido.
</acceptance_criteria>
</task>

</tasks>

## Verification Criteria
1. Fazer login como `pastor`.
2. Preencher o formulário com dados de teste.
3. Clicar em "Cadastrar".
4. Verificar se a mensagem de sucesso aparece.
5. Verificar no banco (`sqlite3 crm.db "SELECT * FROM members;"`) se os dados foram salvos.
